# Production dependencies for Kavak AI Sales Agent

# Web framework
fastapi>=0.104.0,<1.0.0
python-multipart>=0.0.6,<1.0.0  # Required for form data (Twilio webhook)

# Data validation and settings
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0  # Environment variable management

# ASGI server
uvicorn[standard]>=0.24.0,<1.0.0

# LLM integration
openai>=1.12.0,<2.0.0

# Note: Twilio WhatsApp integration uses standard library modules only
# (hmac, hashlib, base64, urllib.parse) - no additional packages required

